#include "main.h"
/**
 *main
*/
void times_table(void)
{
	int n;
	int k;
	int res;

	for (n == 0; n <= 9; n++)
	{
		for (k == 0; k <= 9; k++)
		{
			res = n * k;
			putchar(res);
			putchar(',');
			putchar(' ');
		}
	}
	return(0);
}
