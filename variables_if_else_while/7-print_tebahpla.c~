#include <stdio.h>
/**
 *Excercise two - project: C - Variables, if, else, while
 *Description: prints the alphabet in lower case
 *Return: Always (0);
 */
int main(void)
{
	char lc;

	for (lc = 'z'; lc <= 'a'; lc--)
	{
		putchar(lc);
	}
	putchar('\n');
	return (0);
}
