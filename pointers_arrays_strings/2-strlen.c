#include <string.h>
/**
 *_strlen - returns the numbers of elements in a string
 *@s: the string of characters
 *Description: return the large of string
 *Return:return a number
*/
int _strlen(char *s)
{
	int len;

	len = strlen(s);
	return (len);
}
